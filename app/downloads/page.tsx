'use client';
import { useSearchParams } from 'next/navigation';
const collections = ['NIHOT-回收再生', 'OPTIMUM-食材分選', 'PROMIX-塑膠化工', 'XAVIS-X光檢測'];
export default function DownloadsPage() { const collection = useSearchParams().get('collection'); return <main className="detail-page"><header className="detail-nav"><a href="/">← 回到首頁</a><img src="/reference/logo.png" alt="UniRise 合軒科技有限公司" /><a href="/inquiry">詢價系統</a></header><section className="detail-hero"><div><p>DOWNLOAD CENTER</p><h1>{collection || '下載專區'}</h1><span>型錄與產品資料</span></div></section><section className="detail-shell"><div className="download-panel"><h2>{collection ? `${collection} 資料索取` : '選擇產品資料'}</h2><p>原網站設有下列產品資料分類。為確保資料為最新版本，請透過詢價系統索取。</p>{collection ? <a className="detail-cta" href={`/inquiry?product=${encodeURIComponent(collection)}`}>索取產品資料 →</a> : <div className="download-list">{collections.map((item) => <a href={`/downloads?collection=${encodeURIComponent(item)}`} key={item}>{item}<span>→</span></a>)}</div>}</div></section></main>; }
