'use client';
import { useSearchParams } from 'next/navigation';
import { OriginalFooter, OriginalHeader } from '../original-shell';
const collections = ['NIHOT-回收再生', 'OPTIMUM-食材分選', 'PROMIX-塑膠化工', 'XAVIS-X光檢測'];
const downloadTitles: Record<string,string> = {'3853':'NIHOT-回收再生','77':'OPTIMUM-食材分選','3854':'PROMIX-塑膠化工','78':'XAVIS-X光檢測'};
export default function DownloadsPage() { const params = useSearchParams(); const collection = params.get('collection') || downloadTitles[params.get('id') || '']; return <main id="top" className="original-home original-inner-page"><OriginalHeader/><section className="original-sub-banner"><img src="/reference/original/subbanner.png" alt=""/></section><section className="original-inner-content"><nav className="original-breadcrumb"><a href="/">HOME</a><span>/</span><strong>下載專區</strong></nav><div className="original-inner-title"><span>Download</span><small>{collection || '下載專區'}</small></div><div className="original-content-panel download-panel"><h2>{collection ? `${collection} 資料索取` : '選擇產品資料'}</h2><p>原網站設有下列產品資料分類。為確保資料為最新版本，請透過詢價系統索取。</p>{collection ? <a className="original-inquiry-button" href={`/inquiry?product=${encodeURIComponent(collection)}`}>索取產品資料</a> : <div className="download-list">{collections.map((item) => <a href={`/downloads?collection=${encodeURIComponent(item)}`} key={item}>{item}<span>→</span></a>)}</div>}</div></section><OriginalFooter/></main>; }
